# Temizlik — 28.08.2026

`DURUM_26082026.md`'nin devamı. O belge hataları ve çözümlerini anlatıyor; bu
belge yalnız **hangi dosyanın nereye gittiğini** kaydeder.

Gerekçe: v3 modelleri gerçek hücrede istenen davranışı verdi ve öyle kalacak.
Paket tek kuşak taşıyor; geri kalan her şey burada.

Depo git'e alınmadığından (paket kökünde takipli dosya yok) silmeler geri
alınamaz. Bu yüzden silinen her şey ya **tek komutla yeniden üretilebilir** ya
da **başka bir dosyanın birebir kopyasıdır**. Aşağıda hangisi olduğu yazıyor.

Toplam: **8,4 GB → 4,3 GB**. Kalanın 3,5 GB'ı ham kayıt.

**Konum (28.08.2026, temizlikten sonra):** yedek paketin dışına alındı, artık
`~/Desktop/backup_anomaly_detection`. Paketin çalışma zamanı buna bağlı
DEĞİLDİR — dedektör yedeğe hiç bakmaz, `colcon build` ve düğüm yedeksiz
çalışır. Bağlı olan tek şey `docs/` içindeki makale üreteçleridir; yeri
sabitlemek yerine ararlar (`makale_uret.find_backup`) ve `AD_BACKUP` ortam
değişkeniyle gösterilebilir. Hat betikleri (`run_pipeline_v3.sh` vb.) pakete
mutlak yolla bakar (`AD_PACKAGE`), o yüzden yedeğin yeri onları etkilemez.

---

## 1. Pakette artık ne var

`~/colcon_ws/src/anomaly_detection/` yalnız v3 çalışma zamanını taşıyor (28 MB):

```
residual_ae_v3/  raw_ae_v3/  fusion_v3/  resources/
current_to_torque.json  residual_calibration_fric.json  friction_model.json
anomaly_detection/  launch/  docs/
```

`raw_ae_v2/`, `residual_ae_v2/`, `fusion_v2/`, `residual_calibration_clean.json`
buraya taşındı. Paketteki v2 dizinleri ONNX'i, buradakiler `.pt` + eğitim
eğrisini tutuyordu; birleştirildiler. Paketten gelen iki dosya ad çakışması
olmasın diye `_paket` sonekiyle duruyor:

- `fusion_v2/fusion_config_paket.json`
- `fusion_v2/replay_results_paket.json`

`setup.py` ve `launch/detector.launch.py`'den v2 dalı kaldırıldı. `model_gen`
argümanı **artık yok** — tek kuşak kaldığı için 26.08.2026'daki sürtünme
uyuşmazlığı (model yolları v2'yi, `friction_model` v3'ü gösteriyordu) yapısal
olarak imkânsız.

`detector_node.py` içindeki `fusion_v2` yoklaması **duruyor**: 28.08.2026'dan
eski, yalnız v2 taşıyan bir kurulumu yakalamak için.

## 2. Silinenler — yeniden üretilebilir

Hepsi `backup_anomaly_detection/anomaly_detection/` içindeydi.

| Silinen | Boyut | Geri getiren komut |
|---|---|---|
| `injected_val/`, `injected_test/` | 1,3 GB | `python3 make_injected_features.py --split val --out-dir injected_val` (ve `--split test`) |
| `ur10e_features_nofric.parquet` | 160 MB | `python3 generate_residuals.py --raw ur10e_clean_nm.parquet --out ur10e_features_nofric.parquet --calib-out residual_calibration_nofric.json --resources $RES --backend so --units nm --calibrate offset --fts-frame tool --friction none` |
| `erratum/residual.parquet` | 416 MB | `bash reproduce_erratum.sh` |
| `ur10e_features.parquet` (v1/v2) | 160 MB | `bash run_pipeline.sh` |
| `ur10e_raw_features_nm.parquet` | 182 MB | `python3 convert_effort_to_nm.py` |
| `ur10e_hybrid_residual.parquet` | 271 MB | `python3 generate_residuals.py` (v1 çıktı adı) |

`$RES` = `~/colcon_ws/src/anomaly_detection/resources`.

Sürtünme ablasyonunun **modelleri ve sonuçları** (`*_ae_nf_s0..s4/`,
`fusion_nf_s*/`, `fusion_nf_summary.json`) duruyor — silinen yalnız girdisi.
Aynısı erratum için de geçerli: `erratum/{raw_ae,residual_ae,fusion,calibration.json}` duruyor.

## 3. Silinenler — kopya veya çöp

| Silinen | Boyut | Neden |
|---|---|---|
| `injected_nf_val/`, `injected_nf_test/` | 1,2 GB | Hiçbir betikten referansı yok. İptal edilmiş bir koşunun artığı. |
| `ad_ubuntu.zip` | 452 MB | 18.08.2026 anlık görüntüsü. İçindeki her şey (`belgeler/`, `binaries/`, `fusion_results/`) ağacın kendisinde, daha yenisiyle. |
| `binaries/` | 97 MB | `UR10e_InverseDynamics.fmu` içinde birebir var (`unzip -l` ile doğrulandı). Ayrıca hiçbir Python dosyası okumuyordu; `--backend so` `resources/ur10_solver_py…so`'yu kullanıyor. |
| `check_fmu.parquet`, `check_so.parquet`, `raw_smoke.parquet` | 17 MB | 18.08.2026 duman testi çıktıları; referansı yok. |
| `ur10e_anomaly_detection/` | 68 KB | Yalnız eski paketin `.pyc` dosyaları. |
| `codes/` | — | Boştu. |
| `__pycache__/` (tümü) | 48 KB | Derleme artığı. |

**Not:** `00_ortam_kontrol.sh` hâlâ `binaries/` ve `modelDescription.xml`
varlığını kontrol ediyor; ikisi de artık yalnız `.fmu` içinde. O betiğin bu iki
kontrolü başarısız olur — betiğin kendisi 18.08.2026'dan kalma, düzeltilmedi.

## 4. KORUNANLAR

- **`ros-joint-states.csv` (3,5 GB) — tek ham kayıt.** Hattın gerçek kaynağı;
  `extract_raw_features.py` bunu okur. Silinirse yeni bir kanal türetmek
  imkânsız hâle gelir, hiçbir betikten geri gelmez. Yer sorunu olursa çalışma
  alanının dışına taşınabilir (colcon `src/` altını tarar), ama silinmemeli.
- `ur10e_raw_features.parquet` (188 MB) — hattın giriş noktası
  (`run_pipeline_v3.sh` adım 1). CSV'den türetilebilir ama çıkarım yavaş.
- `ur10e_features_fric.parquet` (161 MB) — **v3 eğitim girdisi**.
- `ur10e_clean.parquet`, `ur10e_clean_nm.parquet`, `splits.json` — bölme ve
  değerlendirme bunları okur.
- Tüm `*_s0..s4/` model dizinleri ve `fusion_*` sonuçları — makalenin kanıtı.
- `Dosya.docx` — **derginin şablonu**, `makale_uret.py` bunu okur. Silinirse
  makale üretilemez.
- `belgeler/245.pdf` — bildiri; `makale_figurler.py` Şekil 1'i bundan çıkarıyor.
- `UR10e_InverseDynamics.fmu` — FMU arşivi. **Değiştirilmedi.**

## 5. Yol değişen dosyalar

| Dosya | Değişiklik |
|---|---|
| `docs/makale_figurler.py` | `fusion_v2/fusion_config.json` → `BACKUP/fusion_v2/fusion_config_paket.json`; `real_system.png` → `figures/_arsiv/`; `fig7_interface.png` artık `_arsiv/`'e yazılıyor |
| `verify_online_features.py` | `--features` varsayılanı `ur10e_features.parquet` → `ur10e_features_fric.parquet` |
| `docs/figures/_arsiv/` | Makalenin 5 şeklinde kullanılmayan görseller (bkz. oradaki README) |
| `belgeler/teknik_rapor/` | `rapor_uret.py`, `sekil_uret.py`, teknik rapor docx ve `sekil_*.png` |

`models.py`'deki `default_parquet="ur10e_features.parquet"` **bilerek
değiştirilmedi**: o bir girdi yolu değil, eğitilmiş modellerin
`metadata.json`'ına yazılan tarihsel kayıttır.

## 6. Bir yan düzeltme

`ros2 run anomaly_detection detector` (launch'suz) **çevrimdışı** birleşim
config'ini alıyordu — 26.08.2026'da kararların %58'ini alarm yapan eşik.
Launch dosyası doğru olanı veriyordu, düğümün kendi varsayılanı vermiyordu.
`detector_node.py`'ye `_cell_or_offline_config()` eklendi: `fusion_config_cell.json`
varsa onu seçer, yoksa çevrimdışına düşer ve stderr'e gürültülü uyarı basar.

Doğrulandı — iki yol da artık θ = 5,0 ve sınır 25,92 / 22,37 raporluyor.

## 7. Temizlikten sonra doğrulananlar

- `colcon build --packages-select anomaly_detection --symlink-install` → temiz
- Kurulan `share/` dizininde v2 kalıntısı yok
- Launch'un yedi çalışma zamanı yolunun tamamı var olan dosyaya çözülüyor
- Düğüm açılıyor: kalıntı θ=1,3723 · ham θ=0,3934 · birleşim θ=5,0 · sürtünme=var
- `makale_uret.py` ve `makale_uret_tr.py` → 5 şekil, 15 tablo, 6 denklem (ikisi de)
- `makale_figurler.py` → fig1–fig4 üretiliyor, çevrimdışı küme 6.016 pencere / 553 arızalı
