# Eğitim + değerlendirme ortamı (normal python3)
numpy>=1.24
pandas>=2.0
pyarrow>=14
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
onnx>=1.15
# torch ayrı kurulur:
#   python3 -m pip install torch --index-url https://download.pytorch.org/whl/cu124

# Ters dinamik ortamı (python3.10 / venv310) için ek olarak:
#   pip install numpy pandas pyarrow scipy fmpy pyzmq
